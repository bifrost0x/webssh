# Web SSH Terminal Configuration
# Copy this file to .env and adjust values

DEBUG=False
HOST=0.0.0.0
PORT=5000
DATA_DIR=/app/data

# SECURITY: Generate a unique key for production!
# Use: python -c "import secrets; print(secrets.token_hex(32))"
# Or: openssl rand -hex 32
SECRET_KEY=<GENERATE-A-SECURE-KEY>

# Allowed origins for CORS (comma-separated, no spaces)
# For production: set to your actual domain(s), e.g., https://ssh.example.com
# For homelab: you can use * with ALLOW_CORS_WILDCARD=true
CORS_ORIGINS=https://ssh.yourdomain.com

# Set to true to allow CORS wildcard (*) in non-debug mode (e.g., homelab use)
# Only enable this in trusted networks!
ALLOW_CORS_WILDCARD=false

# Set to 1 if running behind a reverse proxy (Traefik, nginx, etc.)
# This ensures X-Forwarded-For headers are trusted for rate limiting
TRUSTED_PROXIES=0
